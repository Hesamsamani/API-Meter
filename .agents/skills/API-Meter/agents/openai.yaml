interface:
  display_name: "API Meter"
  short_description: "Repo-specific patterns and workflows for API-Meter"
  default_prompt: "Use the API-Meter repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true